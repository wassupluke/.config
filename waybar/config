{
    "layer": "top",
    "position": "bottom",
    "height": 15,
    "modules-left": ["custom/powermenu", "sway/mode", "sway/workspaces", "sway/window", "custom/music"],
    "modules-center": [],
    "modules-right": ["pulseaudio", "network", "battery", "tray", "idle_inhibitor", "clock"],
    "sway/workspaces": {
        "disable-scroll": true,
        "all-outputs": true,
        "format": "{name}"
    },

    "idle_inhibitor": {
        "format": "{icon}",
        "format-icons": {
            "activated": "☕️",
            "deactivated": "🕶️"
        }
    },

    "sway/mode": {
        "format": "{}"
    },

    "tray": {
        "icon-size": 16,
        "spacing": 7
    },

    "clock": {
        // Human Friendly
        "format-alt": "{:%A, %B %d at %I:%M %p}",
        // 24hr Robot Friendly
        "format": "{:%Y-%m-%d - %H:%M}"
    },
    "cpu": {
        "format": "⚙️ {usage}%",
        "on-click": "alacritty -t 'Floating Terminal' -e btop"
    },

    "memory": {
        "interval": 30,
        "format": "📦️ {used}G"
    },

    "battery": {
        "interval": "60",
        "format": "🔋 {capacity}% ({time})"
    },

    "network": {
        "format-wifi": "📶 {essid} : {signalStrength}%",
        "format-ethernet": "{ifname}",
        "format-disconnected": "Disconnected",
        "on-click": "alacritty -e 'nmtui'"
    },

    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-bluetooth": "{icon} {volume}%",
        "format-muted": "🙊",
        "format-icons": {
            "headphones": "",
            "handsfree": "",
            "headset": "",
            "phone": "",
            "portable": "",
            "car": "",
            "default": ["🔈️" , "🔉", "🔊"]
        },
        "on-click": "pavucontrol"
    },

    "custom/music": {
        "format": "{}",
        "max-length": 20,
        "interval": 5,
        "exec": "$HOME/.config/waybar/mediaplayer.sh 2> /dev/null",
        "tooltip": false,
        "on-click": "playerctl play-pause",
        "on-scroll-up": "playerctl previous",
        "on-scroll-down": "playerctl next"
    },

    "custom/powermenu": {
        "format": "",
        "on-click": "exec ~/.config/waybar/scripts/powermenu.sh"
    }
   }
}
